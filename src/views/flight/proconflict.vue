<template>
  <div>proconflict</div>
</template>
<script>
import { getUserMenuHeader, getSystemUserInfo } from "@/api/system/user";
export default {
  created() {
    // getSystemUserInfo().then((res) => {});
    // getUserMenuHeader().then((res) => {});
  }
};
</script>
